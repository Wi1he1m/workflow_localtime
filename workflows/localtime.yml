name: localtime
on: workflow_dispatch

jobs:
  get_localtime:
    name: Get Localtime
    runs-on: ubuntu-latest
    steps:
      - name: first_step
        run: |
          LOCALTIME=$(date | cut -d' ' -f5)
          echo "$LOCALTIME"
